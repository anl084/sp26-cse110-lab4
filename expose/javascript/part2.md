1. it will just print the length of prices or prices.length()
2. it would print the discountedPrice last ran in the forloop
3. it would print the final price last ran in the forloop
4. it would return a discout list of floating point integers
5. reference error as outside of forllop declaration and use of let
6. reference error as outside of forlooop declarartion
    and use of let
7. it would print the last final price calculated in the forlloop
8. it would return a list with final prices calculated in the forloop.
9. reference error as let doesnt allow outside of scope  usage
10. it would just print the length which is prices.length
11. it would return the discounted list
12. a) student.name;
    b) student["Grad Year"];
    c) student.greeting();
    d) student["Favorite Teacher"].name;
    e) student.courseload[0];
13. a) '32'
    b) 1
    c) 3
    d) '3null'
    e) 4
    f) 0
    g) '3undefined'
    h) Nan
14. a) true
    b) false
    c) true
    d) false
    e) false
    f) true
15. == can do type converison, aka converting type before compare
    === is no type conversion at all so it has to equal in order to work like 1 == 1

17. So when the function gets called, we create a new array and then what we do is then push back the callback of an array item, and do this for each item. Since the callback is num * 2, it then first times the number by 2 return it to where the function1 is at, then we push it into the array. It does this for each item hence returning a [2,4,6]
19. 
4 
3
2
